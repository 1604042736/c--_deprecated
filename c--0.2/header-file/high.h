#pragma once

#include "globals.h"
#include "compiler.h"

char* add(char* one,char* two);
char* sub(char* one,char* two);
char* mul(char* one,char* two);
char* _div(char* one,char* two);
char* mod(char* one,char* two);
